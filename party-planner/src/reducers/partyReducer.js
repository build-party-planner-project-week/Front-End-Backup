const initialState = {
  theme: "",
  guests: "",
  budget: 100,
  date: "",
  todoList: [],
  shoppingList: []
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
